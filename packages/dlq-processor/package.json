{
  "name": "@weather-history/dlq-processor",
  "version": "1.0.0",
  "description": "Lambda function to send email notifications from DLQ messages",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc && npm run copy-deps",
    "copy-deps": "cp -r ../../node_modules ./dist/"
  },
  "dependencies": {
    "@aws-sdk/client-ses": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
